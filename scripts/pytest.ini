[pytest]
# -ra: show a short summary of skips/failures at the end.
# -q : quieter output.
addopts = -ra -q
testpaths = tests
markers =
    integration: trains a real model; requires xgboost and/or lightgbm installed
